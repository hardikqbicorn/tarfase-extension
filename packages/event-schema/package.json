{
  "name": "@ide-collector/event-schema",
  "version": "0.1.0",
  "description": "Canonical, versioned IDE event schema shared by every extension and backend service.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "test": "vitest run"
  },
  "dependencies": {
    "uuid": "^9.0.1",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/uuid": "^9.0.8",
    "typescript": "^5.5.4",
    "vitest": "^2.0.5"
  }
}
